package fr.ynov.dap.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;


@RestController
@RequestMapping(value="/contact")
public class GoogleContactController {
	/*
	 * Automatically injected attribute by the Autowired annotation
	 *
	 * @Autowired private ContactService contactService;
	 * 
	 * /**
	 * 
	 * @param userID Id of the user to access data
	 * 
	 * @return The request response
	 * 
	 * @throws Exception Map the path /count to the associated service method
	 *
	 * @RequestMapping(value="/count") public MasterModel getNbContact(@RequestParam
	 * final String userID) throws GeneralSecurityException, IOException { return
	 * contactService.getNbContacts(userID); }
	 */
}
