package fr.ynov.dap.controller;

import org.springframework.stereotype.Controller;

@Controller
//TODO bal by Djer |POO| Classe à supprimer ? 
public class GoogleAccountController {
	/*
	 * @Autowired private GoogleAccountService googleAccountService;
	 * 
	 * 
	 * 
	 * @RequestMapping("/oAuth2Callback") public String oAuth2Callback(@RequestParam
	 * final String code, final HttpServletRequest request, final HttpSession
	 * session) throws ServletException, GeneralSecurityException { return
	 * googleAccountService.oAuthCallback(code, request, session); }
	 * 
	 * @RequestMapping(value="/account/add/{accountName}", method =
	 * RequestMethod.GET) public String addAccount(@PathVariable final String
	 * accountName,@RequestParam String userKey, final HttpServletRequest request,
	 * final HttpSession session) throws GeneralSecurityException { return
	 * googleAccountService.addAccount(accountName, request, session); }
	 * 
	 * @RequestMapping(value="/account/add/{accountName}") public String
	 * addAccount(@PathVariable final String accountName,@RequestParam String
	 * userKey, final HttpServletRequest request, final HttpSession session) throws
	 * GeneralSecurityException { return
	 * googleAccountService.addAccount(accountName,userKey, request, session); }
	 */
}
